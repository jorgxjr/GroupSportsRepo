@model IEnumerable<GroupSports.DL.DM.trainingPlan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
<div class="content-page">
    <p style="margin-top:30px; margin-left: 15px; margin-right: 15px ">

    </p>
    @*<p  style="margin-top: 30px; margin-left: 15px; margin-right: 15px">
        @Html.ActionLink("Nuevo Plan", "Create")
    </p>*@
    <table class="table" style="margin-left: 15px">
        <tr>
            <th>
                Nombre
            </th>
            <th>
                Fecha Inicio
            </th>
            <th>
                Fecha Fin
            </th>
            <th>
                Atleta
            </th>
          
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.startDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.endDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.athelete.user.username)
                </td>
                @*<td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Detalles", "Details", new { id = item.id }) |
                    @Html.ActionLink("Borrar", "Delete", new { id = item.id }) |
                    @Html.ActionLink("Mesociclos", "Index", "Mesocycles", new { id = item.id }, item)
                </td>*@
            </tr>
        }

    </table>

</div>